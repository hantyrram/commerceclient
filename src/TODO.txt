Decide if we'll separate pricing,inventory requests or on one sinlge product update

Add the newly added product to the store so that we can reference the store instead of the location state,this way 
the product is updated when we update, e.g. setting price.

Set each button that changes screen to be a navigation button which fires a 'nav' action so that we can remove message box

pre-fetch data on app2.js