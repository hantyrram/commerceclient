
schemaValidator, fix text on validation error make it clear
fix product schema what properties we need

refreshing the page, can't redirect to last path because the routes aren't yet defined, redirect is on the admin page

create a pattern for the shipping cost on ShippingMethod.json
Add a preview of a product
Add product photos

!!!was able to login, even with redis = down

Create a product image displayer component
   - A previewer with clickable thumbnails in the bottom
Fix product schema follow documentation


unauthorized still displays admin page,
