import React from 'react';

const style = {
   display: "flex",
   justifyContent: "center",
   alignItems: "center",
   minHeight: "100%",
   minWidth: "100%",
   color: "red"
};

export default ()=>{
   return(
      <div style={style}>
         <span>Feature is under development!</span>
      </div>
   )
}